# 🔑 토큰 노드 아키텍처 개선

## 📋 개요

**통합 토큰 관리 시스템 구현**:

- ✅ HFTokenNode에 **직접 토큰 입력 필드** 추가
- ✅ ModelLoaderNode & DatasetLoaderNode에서 **직접 입력 필드 제거**
- ✅ **다운로드/로드 시만 토큰 포트 표시**
- ✅ 깔끔한 토큰 관리 구조

---

## 🔑 토큰 노드 (HFTokenNode)

### 기능

```
┌─────────────────────────────┐
│  🔑 HF Token Node           │
├─────────────────────────────┤
│ 토큰 종류: User / Fine-grained
│ 토큰 입력: [직접 입력 필드]  │  ← ✨ 새로 추가!
│ 권한 수준: Read / Write / Admin
│ 기본으로 저장: ☑ 체크박스
├─────────────────────────────┤
│         Token (출력)        │  → 다른 노드와 연결
└─────────────────────────────┘
```

### 직접 토큰 입력

```json
{
  "id": "tokenInput",
  "type": "password",
  "label": "토큰 입력",
  "placeholder": "hf_로 시작하는 토큰을 여기에 입력하세요",
  "description": "직접 토큰을 입력하거나 노드 연결을 통해 받을 수 있습니다"
}
```

**동작**:

```typescript
// tokenInput이 변경되면 token도 함께 업데이트
if (parameterId === "tokenInput" && value) {
  updates.token = value;
}
```

---

## 🤖 모델 로더 노드 (ModelLoaderNode)

### 이전 구조

```
ModelLoaderNode
├─ Source: HuggingFace / Local
├─ Model ID: [텍스트 입력]
└─ Access Token: [직접 입력]  ← ❌ 제거됨!
```

### 새로운 구조

```
┌────────────────────────┐
│  🤖 Model Loader       │
├────────────────────────┤
│ Source: HuggingFace    │
│ Model ID: gpt2         │
│ (토큰 필드 없음)        │
└────────────────────────┘
     ↑ 입력 포트 (Token)   ← ✨ 다운로드 시만 표시!
     │ (HFTokenNode과 연결)
     │
```

### 토큰 포트 (조건부 표시)

**JSON 정의**:

```json
{
  "inputs": [
    {
      "id": "in-token",
      "name": "Token",
      "type": "token",
      "optional": true,
      "visible": "download" // ← "huggingface" 또는 "upload"일 때만 표시
    }
  ]
}
```

**Node.tsx 렌더링 로직**:

```typescript
.filter(p => {
  if ((p as any).visible === 'download') {
    // ModelLoader: HuggingFace 다운로드 시만
    return node.data?.source === 'huggingface';
  }
  return true;
})
```

### 토큰 사용 (자동)

```typescript
// 연결된 토큰만 사용
const response = await fetch(`http://localhost:8001/model/download-stream?model_id=${modelId}&access_token=${connectedToken || ""}`, { method: "POST" });
```

---

## 📊 데이터셋 로더 노드 (DatasetLoaderNode)

### 이전 구조

```
DatasetLoaderNode
├─ Source: Local / HuggingFace / Upload
└─ (토큰 입력 필드 없었음)
```

### 새로운 구조

```
┌────────────────────────┐
│  📊 Dataset Loader     │
├────────────────────────┤
│ Source: HuggingFace    │
│ Dataset ID: wikitext   │
└────────────────────────┘
     ↑ 입력 포트 (Token)   ← ✨ 다운로드 시만 표시!
     │ (HFTokenNode과 연결)
```

### 토큰 포트 (조건부 표시)

```json
{
  "inputs": [
    {
      "id": "in-token",
      "name": "Token",
      "type": "token",
      "optional": true,
      "visible": "download" // ← "huggingface" 또는 "upload"일 때만 표시
    }
  ]
}
```

---

## 🔄 워크플로우 구조

### 기본 구조

```
        ┌──────────────────────┐
        │  🔑 HF Token Node    │
        │ (토큰 직접 입력)      │
        └──────────┬───────────┘
                   │ 황금색 선 (토큰)
                   │
        ┌──────────▼───────────┐
        │ 🤖 Model Loader      │
        │ (HuggingFace 다운로드)
        │ ✅ 토큰 연결됨       │
        └──────────┬───────────┘
                   │ 파란색 선 (모델)
                   │
        ┌──────────▼───────────┐
        │ 🎓 Training Node     │
        │ (모델로 학습)        │
        └──────────────────────┘
```

### 다중 토큰 사용

```
┌──────────────────────┐
│ 🔑 Token 1           │ (기본 토큰)
│ (사용자 계정)         │
└──────────┬───────────┘
           │
        ┌──▼──┬─────────────────────────┐
        │     │                         │
   ┌────▼──┐ │ ┌─────────────────────┐ │
   │ Model │ │ │ 🔑 Token 2          │ │
   │Loader │ │ │ (별도 계정)          │ │
   └───────┘ │ └─────────┬───────────┘ │
             │           │             │
             │      ┌────▼──────┐      │
             │      │  Dataset  │      │
             │      │  Loader   │      │
             │      └───────────┘      │
             │                         │
             └─────────────────────────┘
```

---

## ✨ 장점

### 1. 깔끔한 UI

- ✅ ModelLoader/DatasetLoader는 토큰 필드 없음
- ✅ HFTokenNode만 토큰 입력 담당
- ✅ 조건에 따라 포트 동적 표시

### 2. 유연한 토큰 관리

- ✅ 다중 토큰 노드 사용 가능
- ✅ 동일 토큰으로 여러 노드 연결
- ✅ 기본 토큰 설정 기능

### 3. 명확한 데이터 흐름

- ✅ 토큰은 별도 노드에서 관리
- ✅ 노드 간 연결로 의존성 표시
- ✅ 시각적으로 명확한 구조

---

## 📊 포트 가시성 조건

### "download" visible 조건

| 노드          | Source      | 포트 표시 | 설명                      |
| ------------- | ----------- | --------- | ------------------------- |
| ModelLoader   | huggingface | ✅ 표시   | 다운로드 필요             |
| ModelLoader   | local       | ❌ 숨김   | 로컬 모델은 토큰 불필요   |
| DatasetLoader | huggingface | ✅ 표시   | 다운로드 필요             |
| DatasetLoader | upload      | ❌ 숨김   | 파일 업로드는 토큰 불필요 |
| DatasetLoader | local       | ❌ 숨김   | 로컬 데이터는 토큰 불필요 |

---

## 🔧 구현 상세

### 1. JSON에 visible 속성 추가

```json
{
  "inputs": [
    {
      "id": "in-token",
      "visible": "download" // 조건부 표시
    }
  ]
}
```

### 2. Node.tsx에서 필터링

```typescript
.filter(p => {
  if ((p as any).visible === 'download') {
    return node.data?.source === 'huggingface' ||
           node.data?.source === 'upload';
  }
  return true;
})
```

### 3. 토큰 감지 및 사용

```typescript
// 연결된 토큰 자동 감지
const tokenNode = findConnectedTokenNode(node.id, nodes, connections);
const token = getTokenValue(tokenNode);

// API 호출 시 사용
const response = await fetch(`...?access_token=${token || ""}`);
```

### 4. HFTokenNode에서 입력 처리

```typescript
if (parameterId === "tokenInput" && value) {
  // tokenInput 필드를 token 필드에 자동 복사
  updates.token = value;
}
```

---

## 🎯 사용 시나리오

### 시나리오 1: HuggingFace 모델 다운로드

```
1. 🔑 HF Token 노드 추가
   └─ 토큰 입력 필드에 "hf_..." 입력

2. 🤖 Model Loader 노드 추가
   └─ Source: HuggingFace
   └─ Model ID: gpt2
   └─ 입력 포트 (Token) 자동 표시 ✨

3. 토큰 노드의 출력 포트 → Model Loader의 입력 포트 연결
   └─ 황금색 선으로 연결

4. 다운로드 실행
   └─ 연결된 토큰이 자동으로 사용됨
```

### 시나리오 2: 로컬 모델 로드

```
1. 🤖 Model Loader 노드 추가
   └─ Source: Local
   └─ Model: my_model (선택)
   └─ 입력 포트 (Token) 숨김 ← 토큰 필요 없음

2. 로드 실행
   └─ 토큰 없이 바로 로드
```

### 시나리오 3: 데이터셋 다운로드

```
1. 🔑 HF Token 노드
2. 📊 Dataset Loader 추가
   └─ Source: HuggingFace
   └─ Dataset ID: wikitext
   └─ 입력 포트 (Token) 자동 표시 ✨
3. 토큰 노드와 연결
4. 다운로드 실행
```

---

## 📁 수정된 파일

```
✅ frontend/src/@nodes/ModelLoaderNode.json
   ├─ accessToken 필드 제거
   └─ inputs에 Token 포트 추가 (visible: "download")

✅ frontend/src/@nodes/DatasetLoaderNode.json
   └─ inputs에 Token 포트 추가 (visible: "download")

✅ frontend/src/@nodes/HFTokenNode.json
   └─ tokenInput 필드 추가

✅ frontend/src/components/Editor/HFTokenNode.tsx
   └─ tokenInput → token 자동 복사 로직 추가

✅ frontend/src/components/Editor/ModelLoaderNode.tsx
   ├─ accessToken 사용 제거
   └─ connectedToken만 사용

✅ frontend/src/components/Editor/Node.tsx
   └─ visible 속성 필터링 추가
```

---

## 🎓 개념 정리

### 이전 방식 (복잡)

```
각 노드마다 토큰 입력 필드
├─ Model Loader의 accessToken
├─ Dataset Loader의 token
└─ Training의 token
→ 중복된 입력, 유지보수 어려움
```

### 새로운 방식 (깔끔)

```
HFTokenNode에서 토큰 중앙 관리
├─ 다른 노드는 토큰 필드 없음
├─ 필요할 때만 포트 표시
└─ 노드 연결로 토큰 자동 전달
→ 일관성 있고 확장 가능
```

---

## ✅ 완료 항목

- ✅ HFTokenNode에 직접 입력 필드 추가
- ✅ ModelLoaderNode에서 accessToken 제거
- ✅ DatasetLoaderNode 정리
- ✅ 토큰 포트 조건부 표시 (visible)
- ✅ Node.tsx 필터링 로직
- ✅ 토큰 자동 감지 및 사용

---

**마지막 업데이트**: 2025-11-08  
**상태**: ✅ 토큰 아키텍처 개선 완료  
**버전**: 4.0.0
